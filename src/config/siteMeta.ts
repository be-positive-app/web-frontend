/**
 * Site SEO & browser title
 *
 * Edit the strings below, commit, and deploy — no other files need changing for
 * basic SEO (title, description, keywords). Values are injected into index.html
 * when you run `npm run dev` or `npm run build`.
 */
export const SITE_META = {
  title: 'Be Positive — Productivity App',
  description:
    'Be Positive is a mobile productivity app to plan your day, stay focused, and build better habits.',
  /** Comma-separated phrases search engines may read (Google largely ignores this; others may use it). */
  keywords:
    'productivity app, daily planner, task manager, habits, focus, Be Positive, mobile app',
} as const
